# AI Smart Gateway

OpenAI-compatible smart routing layer for authorized upstream APIs.

This server now uses New API as the public front door. AI Smart Gateway is the
internal downstream router that New API calls through a single channel named
`Smart Gateway Router`.

Public client configuration:

```text
Base URL: https://api.example.com
API Key: token generated in New API
```

Primary admin UI:

```text
URL: https://api.example.com/
Purpose: users, tokens, quota, logs, public access, New API channels
```

Smart Gateway operations UI:

```text
URL: https://api.example.com/gateway-admin/
Login token: ADMIN_TOKEN from .env
```

In the recommended deployment, root-level OpenAI-compatible API paths such as
`/models`, `/chat/completions`, and `/responses` route to New API's `/v1/*`
API. The legacy `/v1/*` entry is kept for clients that already include `/v1`.
New API calls Smart Gateway through an internal router channel.

Upstream source management:

1. Add authorized upstream keys in New API as channels.
2. Mark real upstream channels with tag `gateway-source`.
3. Keep New API user groups such as `default` and `vip` for users, tokens, and
   subscriptions. `Smart Gateway Router` is a New API channel, not a group.
4. The systemd timer `ai-smart-gateway-sync-newapi.timer` syncs tagged channels
   into Smart Gateway every 5 minutes. You can also click `同步 New API 源池`
   in the Smart Gateway operations UI.
5. Change upstream enable/disable and weight in New API channel management.
   Optional route tags are `gw:primary`, `gw:opportunistic`, `gw:backup`,
   `gw:paid_fallback`, `gw:free`, `gw:metered`, and `gw:paid`.

Core behavior:

- Pull provider model lists, then verify models with real minimal requests.
- Cache provider model lists and cooldown probe failures to avoid wasting token quota on repeated checks.
- Maintain health by API kind, canonical model, provider, and actual model.
- Treat Responses `invalid_request` from weak probes as request-shape-unverified; verify Codex providers with real Codex-shaped requests before marking them unavailable.
- Return only currently healthy models to New API through the router channel.
- Fail over current `/v1/chat/completions` and `/v1/responses` requests to another healthy upstream for the same model.
- Expose `/gateway-admin/` for authenticated route operations.
- Show currently healthy models and model-level health matrix.
- Show Smart Gateway's read-only source-pool strategy generated from New API.
- Keep provider writes disabled by default; manage upstream channels in New API.
- Expose `/gateway-admin/matrix` for authenticated raw model-level health inspection.

## Deploy

```bash
cp .env.example .env
# Fill upstream keys if needed.
docker compose up -d --build
```

## Verify

```bash
scripts/verify.sh
```

Sync New API source channels into Smart Gateway:

```bash
scripts/sync-newapi-router.py --router-groups default,vip
```

Automatic sync is installed as:

```bash
systemctl status ai-smart-gateway-sync-newapi.timer
```

## Docs

- [New API, Sub2API, and Smart Gateway operations](docs/NEWAPI.md)
- [New API 与 Smart Gateway 边界](docs/current-newapi-smart-gateway-boundary.zh-CN.md)
- [最近路由策略回顾](docs/retrospective-routing-review.zh-CN.md)
- [Implementation review](docs/full-api-gateway-implementation-review.md)

## Backups

Provider config generated by `scripts/sync-newapi-router.py` and emergency
provider writes create backups under:

```text
data/config_backups/
```
